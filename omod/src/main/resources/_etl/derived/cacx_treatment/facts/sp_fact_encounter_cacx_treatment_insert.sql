-- $BEGIN
INSERT INTO mamba_fact_encounter_cacx_treatment (encounter_id,
                                               client_id,
                                               encounter_date,
                                               visit_type,
                                               other_visit_type,
                                               hpv_test_done,
                                               hpv_result,
                                               via_done,
                                               why_no_via,
                                               via_procedure_screening_results,
                                               via_vat_procedure_results,
                                               colposcopy_results,
                                               pap_smear_results,
                                               histology_results,
                                               treatment_provided,
                                               other_treatment_provided,
                                               timelines,
                                               reasons_for_postponing,
                                               other_reasons_for_postponing,
                                               other_services,
                                               other_treatment,
                                               referred_out,
                                               reasons_for_referral,
                                               other_reasons_for_referral,
                                               health_facility_referred_to,
                                               service_offered,
                                               date_service_offered,
                                               comments)
SELECT a.encounter_id,
       a.client_id,
       a.encounter_datetime,
       visit_type,
       other_visit_type,
       hpv_test_done,
       hpv_result,
       via_done,
       why_no_via,
       via_procedure_screening_results,
       via_vat_procedure_results,
       colposcopy_results,
       pap_smear_results,
       histology_results,
       treatment_provided,
       other_treatment_provided,
       timelines,
       reasons_for_postponing,
       other_reasons_for_postponing,
       other_services,
       other_treatment,
       referred_out,
       reasons_for_referral,
       other_reasons_for_referral,
       health_facility_referred_to,
       service_offered,
       date_service_offered,
       comments
FROM mamba_flat_encounter_cacx_treatment a;
-- $END